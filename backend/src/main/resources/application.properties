spring.application.name=index-advisor

db.host=${DB_HOST:localhost}
db.port=${DB_PORT:5432}
db.name=${DB_NAME:index_advisor}
db.user=${DB_USER:}
db.password=${DB_PASSWORD:}

cors.allowed-origin=${CORS_ALLOWED_ORIGIN:http://localhost:5173}
