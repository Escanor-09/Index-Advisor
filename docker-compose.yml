services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    env_file:
      - backend/.env

  frontend:
    build: ./index-advisor
    ports:
      - "5173:80"
    depends_on:
      - backend
